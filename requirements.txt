# --- Núcleo ---
Django>=5.0,<5.2
django-environ>=0.11          # variables de entorno (.env)

# --- Base de datos ---
psycopg2-binary>=2.9          # PostgreSQL (en dev puedes usar SQLite sin esto)

# --- Imágenes (logo, fotos si las agregas) ---
Pillow>=10.0

# --- Formularios en plantillas ---
django-widget-tweaks>=1.5

# --- Estáticos / despliegue ---
whitenoise>=6.6
gunicorn>=21.2

# --- Utilidades de fechas (vencimientos, congelaciones) ---
python-dateutil>=2.9

# --- Generación de reportes en PDF ---
reportlab>=4.0
